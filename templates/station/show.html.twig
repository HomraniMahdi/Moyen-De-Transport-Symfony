{% extends 'dashboard.html.twig' %}

{% block title %}Station{% endblock %}

{% block body %}
    <h1>Station</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ station.id }}</td>
            </tr>
            <tr>
                <th>Ref_station</th>
                <td>{{ station.refStation }}</td>
            </tr>
            <tr>
                <th>Nom_station</th>
                <td>{{ station.nomStation }}</td>
            </tr>
            <tr>
                <th>Position_station</th>
                <td>{{ station.positionStation }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-grid gap-2 d-md-block">
        <a class="btn btn-primary" href="{{ path('station_index') }}">back to list</a>
        <a class="btn btn-warning" href="{{ path('station_edit', {'id': station.id}) }}">edit</a>
        {{ include('station/_delete_form.html.twig') }}
    </div>



{% endblock %}
